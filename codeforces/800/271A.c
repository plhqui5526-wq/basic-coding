#include<stdio.h>
#include<string.h>

int check(int y) {
    int array[4];

    snprintf(array, sizeof(array), "%d", y);


}

int main() {
    int y;
    scanf("%d", &y);
    int flag = 1;
    int y1 = y + 1;
    while(flag) {
        
    }
    return 0;
}